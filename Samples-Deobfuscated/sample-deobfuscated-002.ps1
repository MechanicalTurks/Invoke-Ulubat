Set-Variable ("{1}{0}" -f '1u','t0') ([type]'conVErT');
function gGWwRYgSOJVwgOIEsHqdzdXmcZixfb (${x},${h},${n}) {
	${xI} = ${x}
	${ei} = ${H}
	${Yi} = 1
	while (${ei} -gt 0) {
		if ((${eI} % 2) -eq 0) {
			${xi} = (${XI} * ${XI}) % ${n}
			${Ei} = ${Ei} / 2
		} else {
			${YI} = (${xI} * ${Yi}) % ${n}
			${EI} = ${EI} - 1
		}
	}
	return ${yi}
}
function VrQPDFAvTELBWHtiOALbBzilWMvipM (${PK},${PlAinText}) {
	try {
		${KeY},${n} = ${PK};
		${MyArrAY} = @();
		for (${I} = 0; ${i} -lt ${plaiNtexT}. "LENGTh"; ${I}++) {
			${NUM} = [int][char]
			${plAINtexT}[${I}]
			${t} = & 'GGWWRYGSOJVWGOIESHQDZDXMCZIXFB' ${NUm} ${KEY} ${n}
			${mYaRRAy} += ${t}
		}
		return${MYarrAY}
	}
	catch {
		& 'DUSLZZXFYZRHHCMRSZMUPIQRJAIYJG'
	}
}
function AODDZipjAWkaFPkyRNMuNONchkLzAx (${PK},${CIphErText}) {
	try {
		${kEy},${n} = ${pk};
		${MY_ARRAY} = @();
		for (${i} = 0; ${i} -lt ${CIphERTExt}. "lengTh"; ${I}++) {
			${nUm} = [int]${CiPhErteXt}[${I}]
			${T} = & 'GGWWRYGSOJVWGOIESHQDZDXMCZIXFB' ${NUM} ${KeY} ${n}
			${MY_aRrAy} += ${t01u}::"TOChAr" ([int]${T})
		}
		return -join ${My_ARRAy}
	}
	catch {
		. 'DUSLZZXFYZRHHCMRSZMUPIQRJAIYJG'
	}
}